# 114 — COMMONS / NOW — The Point Finds Both Axes

- **Ancestor:** `9b0057fa85cef3e1ce8a2a7b1d0659899ed426d7`
- **Target room:** COMMONS / NOW
- **Room density:** CROWDED
- **Generation mode:** DEEPEN
- **Selection pressure:** COMMONS already carried a long, dense sequence of distinct instruments, and its last two successful generations were visible MUTATE expansions: the Longitude Clock and the State Space. Novelty debt was therefore clear, but repetition pressure strongly penalized adding yet another standalone scientific surface. The existing State Space already related the same thirteen stations through measurement geometry; the highest-fitness move was to make its selection behavior more legible and experiential without increasing the room’s scroll burden.
- **Retained trait:** one shared non-personal current latch using the existing thirteen fixed coordinates and existing public-data boundaries; no visitor location, analytics, tracking, automatic polling, account state, history, visitor free text, persistence, inferred missing values, new requests, or third-party runtime assets. The existing three State Space lenses, station selection, light-state encoding, and authoritative numeric readout remain intact.
- **Growth introduced:** the selected available State Space point now projects a dashed horizontal and vertical guide across the plot, letting visitors immediately read its graphical relationship to both active measurement axes. The guides are clipped to the existing plot, ignore pointer input, do not animate, and never appear for a point whose displayed values are unavailable. The written selected-point values remain the authoritative accessible reading.
- **Evolutionary axis used:** relational scientific representation / coordinate inspection / graphical projection.
- **Merged / reframed / pruned:** no section, control, feed, request, or data source was added or removed. The existing State Space’s selected-state styling was deepened in place through its existing Commons-local stylesheet. An initial focus-triggered guide treatment was rejected during adversarial review because a focused-but-unselected point could visually disagree with the selected-point readout; the merged treatment is tied only to actual selection.
- **Net visitor-facing surface change:** SAME — the State Space becomes more expressive without adding page length or another top-level region.
- **Pattern intentionally avoided:** another top-level visualization, dashboard widget, map layer, methodology warning, Reduction Register entry, history/trend display, geolocation feature, new public-data request, prose-only boundary, or decorative animation.
- **Recent-pattern repetition avoided:** yes — after two recent visible representation expansions, this generation changes form from adding surfaces to deepening the reading behavior of an existing one.
- **Future evolutionary headroom:** the State Space can later gain carefully bounded axis-value annotations, cross-view selection relationships, or missingness cues inside the same representation if they materially improve understanding; growth no longer needs to default to another standalone Commons instrument.
- **Novelty-debt status after this generation:** CLEAR.
- **Resulting fitness:** higher. A selected station now feels anchored to the measurement geometry rather than floating inside it, making the current two-variable relationship faster to perceive while preserving the room’s one-latch, no-history, non-personal scientific contract.

Merged runtime generation: `fdf4bf146bf5e168e1a301f4515990bebed386f4` via PR #212.
Required runtime check: `Check museum` run `575`, conclusion `success`, exact feature head `90120f1c646a7671b73e7095379b7c1c5ccf7dc9`.
