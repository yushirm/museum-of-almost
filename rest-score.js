(function retireRestScoreView(root) {
  'use strict';

  // Generation 162 retires the Rest Score from the visitor journey.
  // The loader intentionally still receives a successful local script load so
  // later Commons modules mount normally. The underlying source values remain
  // available in their original instruments; this layer no longer reclassifies
  // them into a second semantic dashboard.
  if (!root || !root.document) return;
})(typeof globalThis !== 'undefined' ? globalThis : this);
